describe('test', () =>{
       it('should exist a method named "sumArray',() => {
        expect(typeof sumArray).toEqual('fuction')
       })
        it('should it not return null',() => {
            expect(sumArray,value).not.toBe('null')
        })
        it('should it not return undefined', () => {
            expect(sumArray.value).not.toBe('undefined')

        })


})